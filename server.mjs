import http from 'node:http';import fs from 'node:fs';import path from 'node:path';import {fileURLToPath} from 'node:url';
const root=path.dirname(fileURLToPath(import.meta.url));const pub=path.join(root,'public');const data=path.join(root,'uoc-nguyen');fs.mkdirSync(data,{recursive:true});const dbFile=path.join(data,'wishes.json');
const read=()=>{try{return JSON.parse(fs.readFileSync(dbFile,'utf8'))}catch{return[]}};
const write=a=>{fs.writeFileSync(dbFile,JSON.stringify(a,null,2),'utf8');const q=v=>'"'+String(v??'').replaceAll('"','""')+'"';fs.writeFileSync(path.join(data,'uoc-nguyen.csv'),'\uFEFFSTT,Thời gian,Tên / thiết bị,Ước nguyện\r\n'+a.map((w,i)=>[i+1,w.created_at,w.sender_name||'Ẩn danh',w.message].map(q).join(',')).join('\r\n'),'utf8');fs.writeFileSync(path.join(data,'uoc-nguyen.txt'),a.map((w,i)=>`${i+1}. [${w.created_at}] ${w.sender_name||'Ẩn danh'} | ${w.message}`).join('\n'),'utf8')};
const mime={'.html':'text/html; charset=utf-8','.css':'text/css; charset=utf-8','.js':'application/javascript; charset=utf-8','.png':'image/png','.jpg':'image/jpeg','.jpeg':'image/jpeg','.mp3':'audio/mpeg','.svg':'image/svg+xml'};
http.createServer((req,res)=>{const u=new URL(req.url,'http://localhost');if(u.pathname==='/api/wishes'&&req.method==='GET'){res.setHeader('content-type','application/json; charset=utf-8');return res.end(JSON.stringify({wishes:read().slice(0,200)}))}if(u.pathname==='/api/wishes'&&req.method==='POST'){let b='';req.on('data',d=>b+=d);req.on('end',()=>{try{const body=JSON.parse(b);const sender_name=String(body.sender_name||body.senderName||'').trim().normalize('NFC')||'Ẩn danh';const m=String(body.message||'').trim().normalize('NFC');if(sender_name.length>60||!m||m.length>160){res.statusCode=400;return res.end('{}')}const a=read();const w={id:Date.now(),sender_name,message:m,created_at:new Date().toISOString()};a.unshift(w);write(a.slice(0,1000));res.setHeader('content-type','application/json; charset=utf-8');res.end(JSON.stringify({ok:true,wish:w}))}catch{res.statusCode=400;res.end('{}')}});return}if(u.pathname==='/api/wishes.csv'){const f=path.join(data,'uoc-nguyen.csv');if(!fs.existsSync(f))write(read());res.setHeader('content-type','text/csv; charset=utf-8');res.setHeader('content-disposition','attachment; filename="uoc-nguyen-trung-thu.csv"');return fs.createReadStream(f).pipe(res)}let p=path.join(pub,u.pathname==='/'?'index.html':decodeURIComponent(u.pathname).replace(/^\//,''));if(!p.startsWith(pub)||!fs.existsSync(p)||fs.statSync(p).isDirectory()){res.statusCode=404;return res.end('Not found')}res.setHeader('content-type',mime[path.extname(p)]||'application/octet-stream');fs.createReadStream(p).pipe(res)}).listen(8787,()=>console.log('http://localhost:8787'));
